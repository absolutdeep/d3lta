# Implementation Plans

Generated by the improve skill on 2026-08-02. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

All plans are written against commit `7b94eca` (the repo's only commit).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Establish a test baseline (Vitest + RTL) | P1 | L | — | DONE |
| 002  | Fix packaging & repo hygiene | P1 | S | — | DONE |
| 003  | Make /api/agents non-blocking | P1 | M | — | DONE |
| 004  | Harden API routes (validation, limits, redaction) | P1 | M | — | DONE |
| 005  | Unify theme application & clear vars on removal | P1 | M | 001 | DONE |
| 006  | Fix CSS/font bugs + dynamic-load Three.js | P2 | S | — | DONE |
| 007  | Remove redundant ThemeContext state layer | P2 | M | 001 | DONE |
| 008  | Fix dnd-kit dropzone highlight | P2 | S | — | DONE |
| 009  | Extract shared polling hook | P2 | S | 001 | DONE |
| 010  | Add security headers (CSP) to next.config | P2 | M | — | DONE |
| 011  | Fix error-handling system (flush + dead code) | P2 | M | 001 | DONE |
| 012  | Move theme-name resolution server-side | P3 | S | — | DONE |
| 013  | Generate DB migrations | P3 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 005 requires 001 because the theme-application refactor touches the store and
  service, which 001 gives characterization tests for.
- 007 requires 001 because it deletes a public-ish API surface (`useTheme`,
  `withTheme`) and removes the `ThemeProvider` wrapper; tests prove nothing
  depended on it.
- 009 requires 001 because it extracts a hook used by two components; the
  characterization tests in 001 cover the current behavior.
- 011 requires 001 because it changes the `Logger` flush behavior and removes
  dead exports; tests lock the new behavior.
- 002, 003, 004, 006, 008, 010, 012, 013 are independent and safe to run in any
  order, and can be done before 001.

## Findings considered and rejected

- **`fetchThemeName` client-side CORS** (finding 17): folded into plan 012
  (move name resolution server-side) rather than a standalone fix.
- **Unused `axios`/`@dnd-kit/sortable`/`@dnd-kit/utilities` deps** (finding 14):
  folded into plan 002 (packaging & hygiene).
- **`shadcn` should be a devDependency** (finding 14): folded into plan 002.
  CORRECTION (2026-08-02): `shadcn` is NOT a dev-only tool here — `app/globals.css`
  does `@import "shadcn/tailwind.css"`, so the package is resolved at build time
  and must remain a runtime `dependency`. Plan 002 therefore removed only the
  genuinely-unused deps (`axios`, `@dnd-kit/sortable`, `@dnd-kit/utilities`) and
  moved `zod` to `dependencies`; `shadcn` was kept as a dependency.
- **`withErrorHandling`/`dbError` dead code** (finding 16): folded into plan 011.
- **`d3lta.db` + `graphify-out/` committed** (finding 3): folded into plan 002.
- **`isActive: Boolean(body.isActive)` coercion** (finding 12): folded into plan 004.
- **`Number(id)` → NaN 500** (finding 12): folded into plan 004.
- **`baseUrl`/`configPath`/`sessionsDir` exposure** (finding 13): folded into plan 004.
- **`--tracking-normal` self-reference + font mismatch** (finding 9): folded into plan 006.
- **Three.js static import** (finding 4): folded into plan 006.
- **Duplicated polling logic** (finding 11): folded into plan 009.
- **`useDroppable` decorative** (finding 10): folded into plan 008.
- **Empty `next.config.ts` / no CSP** (finding 15): folded into plan 010.
- **Logger buffer never flushed** (finding 16): folded into plan 011.
- **No DB migrations** (finding 18): folded into plan 013.